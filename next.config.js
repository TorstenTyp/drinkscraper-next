/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets-prd.punchdrink.com', 'cdn.diffords.com', 'cdn.shortpixel.ai', 'www.liquor.com', 'iba-world.com'],
    },
};

module.exports = nextConfig;
